A tener en cuenta para la GUI:
Ingrese valor se habilita cuando se ingresa una variable. 
Cuando se ingrese el valor y se presione "Enter", se carga la variable en el mapeo "variables" de la forma siguiente:
variables.put(txtA.getText(), textField.getText());
txtA.getText() se debe agregar al JTextPane txtpnVariable y textField.getText() al JTextPane txtpnValor. Para esto hay que tener en cuenta cómo se agrega texto a un JTextPane.

Tener en cuenta cuando se ingrese una variable que si ya está ingresada, debe aparecer un cartel emergente que diga: Esta variable ya se ha guardado, o y está ingresada o algo así. Para esto, es útil observar el método insertarVariable de la clase Programa.

Las operaciones ingresadas:
si lanza un error (cualquiera sea) el programa, es porque o los paréntesis son incorrectos o porque los signos están mal ubicados. Básicamente, porque la operación está mal escrita. Por lo tanto, para cualquier error que lance el programa, debe aparecer el cartel de "expresión incorrecta" o algo así.

Cuando se ingresa la operación (o sea se presiona Enter), se deben cargar las expresiones en prefijo, infijo y postfijo. Para esto, acordarse que el infijo queda el string q está. para prefijo y postfijo, llamar a los métodos prefijoString(string) y postfijoString(string) de la clase programa. Se debe cargar también el resultado de la operación, y los valores de altura del árbol, etc.(métodos implementados: hasta ahora 1, alturaArbol():int)
El resultado(label_2), llama al método calcular, que recibe el string que me da el método postfijoString.
Por otro lado, la clase privada resolver árbol, cuando encuentra un nodo de altura 1, debe reemplazar a la expresión. Lo que habría que hacer es ver cómo el nombre de la nueva variable que ingresa el usuario, es utilizada en esta clase.

Faltan implementar los métodos declarados postorden y preorden, que son los que te pedí hoy.
Falta implementar:
*nro de hojas, nro de nodos del árbol, nro de nodos internos del árbol, que hay que hacerlo desde afuera de la estructura.